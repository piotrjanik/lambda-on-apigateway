version: 2.0
jobs:
  build:
    docker:
      - image: node:10
    steps:
      - checkout
      - run:
          name: build lambda
          command: |
            cd contact-requests-collector
            npm install
            node_modules/.bin/gulp package
